<div class="flex gap-4 rounded-lg border border-gray-200 p-4 dark:border-gray-700">
  <% if (variant = cover_variant) %>
    <%= image_tag variant, class: "h-32 w-auto flex-none rounded object-cover", alt: "" %>
  <% else %>
    <div class="flex h-32 w-[5.5rem] flex-none items-center justify-center rounded border border-dashed border-gray-300 text-center text-xs text-gray-400 dark:border-gray-600 dark:text-gray-500">
      No cover
    </div>
  <% end %>

  <div class="flex min-w-0 flex-1 flex-col gap-0.5">
    <p class="font-medium text-gray-900 dark:text-gray-100"><%= format_line %></p>
    <% if meta_line.present? %>
      <p class="text-sm text-gray-500 dark:text-gray-400"><%= meta_line %></p>
    <% end %>

    <% if identifiers.any? %>
      <div class="mt-2 flex flex-wrap gap-x-3 gap-y-1 font-mono text-xs text-gray-500 dark:text-gray-400">
        <% identifiers.each do |label, value, url| %>
          <span>
            <b class="font-semibold text-gray-400 dark:text-gray-500"><%= label %></b>
            <% if url %>
              <%= link_to value, url, target: "_blank", rel: "noopener", class: "underline hover:text-gray-700 dark:hover:text-gray-200" %>
            <% else %>
              <%= value %>
            <% end %>
          </span>
        <% end %>
      </div>
    <% end %>
  </div>
</div>
